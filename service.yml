---
apiVersion: v1
kind: Service
metadata:
  name: fitness-deployment-svc 
  namespace: thanu-ns
spec:
  selector:
    app: fit
  ports:
    - port: 3001 #it is a service port number we are using app to the internet 
      targetPort: 5000
      protocol: TCP
  type: NodePort  
    